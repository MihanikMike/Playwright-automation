import { test } from "@playwright/test";

test.describe("Test Group", () => {
  test("A", async ({ page }) => {
    // empty test group
  });

  test("B", async ({ page }) => {
    // empty test group
  });

  test("C", async ({ page }) => {
    // empty test group
  });
});
